
<% if user_signed_in? %>
  <%= form_with model: ([@question, @question.answers.build]) do |f| %>
  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>
  <%= f.submit %>
  <% end %>
<% end %>
